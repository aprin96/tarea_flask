[project]
name = "tarea01-flask"
version = "0.1.0"
description = "Tarea 01 - Aplicacion Flask con CRUD de usuarios y API JSON"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "flask-sqlalchemy>=3.1.1",
    "python-dotenv>=1.2.2",
]
